# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Local settings (template only in repo)
settings.local.json

# Per-machine path configuration (written by /core-bootstrap)
.claude/paths.local.yaml

# Project state (project-specific, not shared)
.project/

# Personal overlay (user-owned, never commit to public repo)
personal/

# Playwright artifacts
.playwright-mcp/
.playwright-cli/

# Test screenshots
*.png
*.jpeg
*.jpg

# ...except real source assets (PWA/app icons — served directly, not generated
# test output), regenerable from favicon-backlog.svg via the install-launcher
# reference doc but committed so a fresh clone serves them without that step.
!skills/shared/references/icon-192.png
!skills/shared/references/icon-512.png
